# This fixture sits inside herdr-llm-lint's own git history, which the
# history-based checks would read.
size_bytes = 512
enable = ["shape-body"]
disable = ["drift-stale", "drift-age", "git-untracked", "git-local-tracked"]
denylist = ["my personal"]
